 <div class="row is-centered"> 
       {{#each posts as |post|}}
       <div class="row mb-4 project">
  <div class="col-md-5">
    <h2>
     <{{title}}"></a>
    </h2>
    <p>
    
    </p>
       
        {{/each}}
</div>